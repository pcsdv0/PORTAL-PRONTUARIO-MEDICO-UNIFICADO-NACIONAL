<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Editar Paciente</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2 class="mb-4 fw-bold fw-bold text-light">Editar Paciente</h2>

    <form action="/pacientes/editar/{{paciente.id}}" method="POST">
      <div class="mb-3">
        <label for="nome" class="form-label fw-bold text-light">Nome</label>
        <input type="text" class="form-control" id="nome" name="nome" value="{{paciente.nome}}" required>
      </div>

      <div class="mb-3">
        <label for="data_nascimento" class="form-label fw-bold text-light">Data de Nascimento</label>
        <input type="date" class="form-control" id="data_nascimento" name="data_nascimento" value="{{paciente.data_nascimento}}" required>
      </div>

      <div class="mb-3">
        <label for="telefone" class="form-label fw-bold text-light">Telefone</label>
        <input type="text" class="form-control" id="telefone" name="telefone" value="{{paciente.telefone}}" required>
      </div>

      <div class="mb-3">
        <label for="cpf" class="form-label fw-bold text-light">cpf</label>
        <input type="text" class="form-control" id="cpf" name="cpf" value="{{paciente.cpf}}" placeholder="000.000.000-00" required>
      </div>


      <button type="submit" class="btn btn-primary">Salvar Alterações</button>
      <a href="/pacientes" class="btn btn-secondary">Cancelar</a>
    </form>
  </div>
</body>
</html>
